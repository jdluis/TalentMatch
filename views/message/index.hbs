<div>

    <h2>All Messages:</h2>

    {{#each openChats}}
        <img src="{{this.img}}" alt="{{this.name}}" width="50px">
        <a href="/message/{{this.id}}">{{this.name}} {{this.secondName}}</a>
    {{/each}}

</div>